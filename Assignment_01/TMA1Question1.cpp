//: TMA1Question1.cpp/* Title: TMA1Question1.cpp Description: Counting white-space characters(' ', '\t') with C++ Date: September 25, 2019 Author: Alvee H. Akash ID: 3433334 Version: 1.0 Copyright: 2019 Alvee H. Akash*//* DOCUMENTATION Program Purpose: Documentation followed as GoodDocs.cpp, test plans are showed in the next paragraph, and comments are inline comments for better understanding. keyboard input file name with ext. simple calculations are addition. Compile (Assuming GCC compiler istalled): g++ -o TMA1Question1 TMA1Question1.cpp Execution (assuming Cygwin is running): ./TMA1Question1 Notes: in g++, main return type int Classes: none Variables:     fileName - string - used to store the file name from user.     inputFile - ifstream - used to open the file stream from 'fileName'.     totalWhiteSpace - int - used to store white-space characters (' ', '\t') as an                             integer number*//* TEST PLAN Normal case:      Welcome to whitespace Counting Program      Please provide the file name : Q2_text.txt      Total whitespace in the "Q2_text.txt" file is : 41 Bad Data case 1 (not existing file in the directory)      Welcome to whitespace Counting Program      Please provide the file name : no name file      File can not be opened Bad Data case 2 (invalid file)      Welcome to whitespace Counting Program      Please provide the file name : Alvee Surf.JPG      File can not be opened Limited Condition (file name with extension)      Welcome to whitespace Counting Program      Please provide the file name : Q2_text      File can not be opened Discussion:     The program accepts a file name (as string). Opens the file and reads each character and checks if it is whitespace such as space or tab char.      If it detects one of the whitespace char, it increments totalWhiteSpace variable. In the end of the file, program shows the total whitespace in that file.*/#include <iostream>#include <fstream>using namespace std;int main(void) {    /*     All the variables are initialised here     */    string fileName;    ifstream input_File;    int totalWhiteSpace = 0;    cout << " Welcome to whitespace Counting Program \n\n";    // request and obtain file name    cout << "Please provide the file name : ";    cin >> fileName;    // Opening file    input_File.open(fileName);    // Error check, if file can not be opened, exit program with an error message.    if (!input_File.is_open()) {        cout << "File can not be opened\n" ;        exit(EXIT_FAILURE);    } // if    // loop runs every character in the file    while (!input_File.eof()) {        // check for Whitespace such as ' ', '\t'        if (isblank(input_File.get())) {            // incrementing variable value, after finding whitespace character            totalWhiteSpace++;        } // if    } // while    // Printing the output    cout << "\nTotal whitespace in the \""<< fileName <<"\" file is : " << totalWhiteSpace << endl;    // closing file    input_File.close();} ///:~